classdef(Enumeration) GeneratorStatus < int32
    % GeneratorStatus: enumerated values for Loads.Status
    % 
    % Values:
    %    Variable(0)
    %    Fixed(1)

    enumeration
        Variable(0)
        Fixed(1)
    end
end
